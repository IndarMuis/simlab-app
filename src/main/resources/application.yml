spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/db_simlab
    username: postgres
    password: al0homora
    driver-class-name: org.postgresql.Driver
  jpa:
    database: POSTGRESQL
    hibernate:
    #      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

jwt:
  header: 'Authorization'
  prefix: 'Bearer '
  expiration: 70000
  issuer: 'simlab-app'
  secret: CBE967D33B28E374688ABFA97FDB5